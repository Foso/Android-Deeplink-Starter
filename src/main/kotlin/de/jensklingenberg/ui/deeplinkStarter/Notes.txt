/**
                     * val test =ModuleManager.getInstance(ProjectManager.getInstance().openProjects.first()).modules.get(1)
                    val facet = FacetManager.getInstance(test).getFacetsByType(AndroidFacet.ID)
                    val manifestFile = AndroidRootUtil.getPrimaryManifestFile(facet.first())
                    manifestFile

                    val test =AndroidResourceUtil.findResourceFields(manifestFile!!,"string","mycustom",false)

                     *
                     *
                     *
                     *
                     *
                     * val manifestFile = AndroidFacet.getInstance(p0)


                    val test =AndroidResourceUtil.findResourceFields(manifestFile!!,"string","mycustom",false)


                    test.first().containingClass.qualifiedName
                    test.first()
                    AndroidResourceUtil.findResourcesByField(test.first())
                     *
                     */